# DX-OS Agents

Status: NOT_READY

This repository belongs to DX-OS. See openspec and beads for current state.
Do not perform business work until READY gate passes.

The DX-OS constitution at `.specify/memory/constitution.md` is mandatory and higher authority than lower project artifacts. DX-OS itself is an open-source project. Preserve public-source ownership, the ADR-governed Apache-2.0 default, dependency and reused-source attribution, separate service disclosure, AI provider independence, and fail-closed CLEAN CLONE / READY release gates.
