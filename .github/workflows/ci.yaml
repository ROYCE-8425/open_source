name: CI Quality Gate

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  validate:
    name: Full Quality Gate (Ubuntu Parity)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@0a44ba7841725637a54e280ec667c276c6cb814d # v4.4.1
        with:
          node-version: '22'

      - name: Install OpenSpec CLI
        shell: bash
        run: |
          npm install -g @fission-ai/openspec@1.8.0

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@3951f0dda7a4f37618c59e7e6027a4d538686e09 # v4.3.0
        with:
          dotnet-version: '10.0.302'

      - name: Acquire Pinned Security Tools
        shell: pwsh
        run: |
          ./scripts/setup-security-tools.ps1

      - name: Verify Security Canaries
        shell: pwsh
        run: |
          ./scripts/verify-security-canaries.ps1

      - name: Run Full Quality Gate
        shell: pwsh
        run: |
          ./scripts/check.ps1 -Profile Full

      - name: Upload Quality Gate Evidence
        if: always()
        uses: actions/upload-artifact@4cec3d8aa04e39d1a68397de0c4cd6fb9dce8ec1 # v4.6.1
        with:
          name: quality-gate-evidence
          path: |
            artifacts/quality-gate/
            artifacts/security/
            artifacts/task-runs/
          if-no-files-found: error
