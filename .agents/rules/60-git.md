# Git & Workflow Rules

- Work on main branch during bootstrap remediation following Beads task sequencing.
- No direct push, force push, or history rewriting without independent review PASS and authorization.
- Git working tree must remain clean with locked package restore.
- Do not commit generated artifacts, bin/obj, test container state, or temporary logs.